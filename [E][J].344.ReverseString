class Solution {
    public void reverseString(char[] s) {
        char temp;
        int j;
        for (int i = 0; i < s.length; i++) {
            j = s.length - 1 - i; 
            if (i >= j) break;
            temp = s[i];
            s[i] = s[j];
            s[j] = temp;
        }
    }
}
